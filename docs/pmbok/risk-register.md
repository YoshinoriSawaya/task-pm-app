# リスク登録簿

| ID | リスク内容 | 発生確率 | 影響度 | 優先度 | 対応策 |
|---|---|---|---|---|---|
| R1 | スケジュール超過: WBS([wbs.md](wbs.md))が9フェーズ・約37 Issueに分解された一方、開発期間は1日8時間×2日(計16時間)。額面通り完走するのは困難な可能性が高い。ただし、ここまでの進行速度(Claudeとの対話による要件定義〜設計フェーズの高速な完了)を踏まえると、**Claudeの利用量(Pro等のプラン上限)がオーバーしない限りは、この開発時間自体は大きな問題にならない可能性が高い**。つまり実質的なボトルネックは開発者の稼働時間よりもClaude利用枠にシフトしている。**[change-log.md](change-log.md) C2(EVM進捗指標・バグ管理の追加)により、スコープが新規スライス2つ分さらに拡大しており、本リスクは追加スコープ決定時点で承知の上での再発生とみなす**。フェーズ3完了時点で自己適用EVMのCPI=0.5が実測され、本リスクが顕在化したことを確認([schedule.md](schedule.md))。これを受けて対応策のMust/Should仕分けを実行に移した(**[change-log.md](change-log.md) C12**)。時間は固定・品質(SOLID比較・PMBOK実体験)は死守という前提のもと、動かせるレバーはスコープのみと位置付け、CPI低下自体は「失敗」ではなく「スコープ判断を行うタイミングを示すシグナル」として扱う運用に転換した | 中 | 高 | 高 | 各フェーズの完了時点でMVP必須/Nice to haveを仕分け、時間逼迫時は実装量そのものをスコープカットする。あわせてClaudeの利用量(メッセージ数・使用量上限)を定期的に確認し、枯渇が見えてきた時点で優先度の低いフェーズ(PMBOKドキュメント整備等)を早めに簡略化する。「Before/After設計比較」「ADR」「EVM/バグ管理機能」等、面接アピール価値の高い成果物は優先して死守し、削るならまず`Progress`スライスのUI装飾や`bugs`の細かい属性など、周辺部分から削る。**具体的な仕分け(C12)**: Must=Task画面(#18,#19)・静的解析/最低限のユニットテスト(#20,#21)・主要フローE2E(#25)・リファクタリング設計比較(#29)・デプロイ/セキュリティレビュー(#34,#53)・PMBOKドキュメント整備(#40)。Should=工数入力欄・バグ管理画面・進捗ダッシュボード画面(#47,#48,#49、バックエンドAPIは実装済みのため画面未着手のままでも機能自体は失われない) |
| R2 | AWS予算超過: 本番デプロイを受け入れ基準(DoD)に含めているが、AWSに使える予算は2000円以下。リソース削除忘れ・稼働時間の見積り誤りで超過する可能性。[ADR-0003](../adr/0003-deployment-architecture.md)によりEC2単体(t3.micro、RDS不使用)という具体的な構成が決まったことで、時間単価は概算1〜2円程度に抑えられる見込み | 低 | 中 | 中 | デプロイ後、動作確認が済み次第即座にインスタンスごと削除する運用を徹底(Issue #33)。RDSを使わずEC2単体構成とすることで時間単価を抑える([ADR-0003](../adr/0003-deployment-architecture.md)) |
| R3 | 単一障害点: 開発者が1人のため、体調不良・集中力低下がそのままスケジュール遅延に直結する | 中 | 高 | 中 | 各フェーズの成果物をこまめにコミット・Issueクローズし、途中離脱してもどこまで進んだか即座に追える状態を維持する(現状のドキュメント駆動の進め方自体が対応策になっている) |
