# 変更要求ログ

開発中に発生したスコープ変更・設計判断・発見したバグ/見落としを、発生順に1件1エントリで記録する
(運用ルールは[wbs.md](wbs.md)参照)。プロジェクト完了時点で全25件(C1〜C25)。

## サマリ(プロジェクト完了時点)

- **スコープ・PM上の意思決定**: C1(論理削除への変更)、C2(EVM/バグ管理のスコープ追加)、C3(セルフEVM適用の運用決定)、C9(重複Issue統合)、C12(CPI=0.5を受けたMust/Should仕分け)、C19(CPI改善を受けたShould着手)、C25(AWSリソースの削除vs停止の判断)
- **`/code-review`・security-reviewが発見した実バグ**: C10(論理削除回避・decimal超過等3件)、C14(tsconfig参照漏れ)、C17(healthcheckのソケット/TCP誤判定)、C20(Task/BugPageの状態管理4件)、C22(本番APP_DEBUG漏えいリスク)。いずれも「レビューを1回で終わらせず、直前の修正自体も再度レビュー対象にする」運用(C14, C17)が実際に効果を発揮した
- **CI・環境構築のトラブルと修正**: C5(CIトリガー不備)、C8(スキャフォールド時の複数トラブル)、C11(DB分離漏れ)、C13(`tsc --noEmit`の空振り)、C16(healthcheckタイミング)
- **手動操作・実機検証でのみ発見できた欠陥**: C18(子タスク作成UIの欠落)、C23(`php artisan serve`のHTTPリクエストコンテキストでの環境変数見失い)。いずれも自動テスト(Unit/Feature/E2E)では原理的に検知できない種類の欠陥で、探索的な動作確認・本番相当の起動経路での検証の価値を裏付けた

| ID | 変更内容 | 影響(工数/予算/納期) | 承認状況 |
|---|---|---|---|
| C1 | タスク削除方式の変更(物理削除→論理削除)。[er-diagram.md](../architecture/er-diagram.md)初版では親タスク削除時に子タスクをCASCADE物理削除としていたが、レビューで「誤削除から復旧できない」「監査・振り返りの材料が残らない」との指摘を受け、論理削除(`deleted_at`, Laravel SoftDeletes)に変更。親削除時の子タスクへの伝播もApplication層でのカスケード論理削除に変更 | 工数への影響は軽微(マイグレーション・リポジトリ実装レベル)。予算・納期への影響なし | 承認済み(設計フェーズ内でのレビューにより即時反映) |
| C2 | 「コスト管理(EVM)」をスコープ外からスコープ内に変更。進捗指標(EVM風メトリクス)とバグ管理をアプリ機能として追加([ADR-0002](../adr/0002-evm-progress-and-bug-tracking.md))。`tasks`への工数カラム追加、`bugs`テーブル新設、`Bug`/`Progress`の2スライス追加を伴う | 工数への影響は大きい(新規スライス2つ、新規画面2つ)。[risk-register.md](risk-register.md) R1のスケジュール超過リスクをさらに高める。予算・納期への直接的な影響はないが、時間逼迫時のスコープカット候補になり得る | 承認済み(要求元自身の判断による意図的なスコープ追加) |
| C3 | ADR-0002のEVM計算式を、アプリ機能としてだけでなく開発プロジェクト自身(このtask-pm-app開発)にもセルフドッグフーディングとして適用することを決定。[schedule.md](schedule.md)にEVMサマリ表を追加し、フェーズ完了ごとに実績時間を記録・更新する運用にした | 工数への影響はほぼなし(記録作業のみ)。ただし実績時間の自己申告という新たな運用負荷が発生する | 承認済み(1人・小規模プロジェクトへの厳密なEVM運用は統計的な意味は薄いが、実体験としての価値を優先するとの判断) |
| C4 | 設計フェーズ完了後、命名規則・フォーマッタ・静的解析の厳格度が未定義だったことに気づき、[coding-standards.md](../development/coding-standards.md)を新設。環境構築確認フェーズ(#12)に設定ファイル作成の子タスク([#52](https://github.com/YoshinoriSawaya/task-pm-app/issues/52))を追加 | 工数への影響は軽微(設定ファイル作成のみ)。#16・#21の「静的解析通過」の達成基準が明確になった分、後工程の手戻りリスクはむしろ低減する | 承認済み |
| C5 | コーディング規約を詳細化する過程で、`.github/workflows/*.yml`のCIトリガーが初期スキャフォールドのまま`pull_request`になっており、「PRフローを取らずmainに直接コミットする」というGit規約と矛盾していた(このままではCIが一度も実行されない)ことが判明。`push`(mainブランチ)トリガーに修正し、Pint/Prettierのフォーマットチェック・`tsc --noEmit`の型チェックもCIに追加した。あわせて`backend/.env.example`・`frontend/.env.example`を新設 | 工数への影響はごく軽微(設定ファイル修正のみ)。発見が遅れていた場合、実装フェーズでCIが一度も動いていないことに後から気づくという重大な手戻りになり得た | 承認済み(実装着手前に発見できたため実質的な悪影響はなし) |
| C6 | プロジェクトマネジメント観点での見直しにより、以下の未計画事項を発見・追加。(1)実装系フェーズ完了時の軽量コードレビュー(`/code-review`)をWBS運用ルールに追加、(2)デプロイ前セキュリティレビュー([#53](https://github.com/YoshinoriSawaya/task-pm-app/issues/53))を追加、(3)デモ用シードデータ作成([#54](https://github.com/YoshinoriSawaya/task-pm-app/issues/54))を追加、(4)CORS設定([#55](https://github.com/YoshinoriSawaya/task-pm-app/issues/55))を追加、(5)フロントのエラー表示規約をcoding-standards.mdに追記、(6)MIT LICENSEを新設 | レビュー・セキュリティレビューの追加分はさらにスケジュール(risk-register R1)を圧迫する。シードデータ・CORS・LICENSEは軽微 | 承認済み(レビュー能力のアピールという目的そのものに直結するため、スケジュールリスクより優先する判断) |
| C7 | AWSデプロイ構成を具体化([ADR-0003](../adr/0003-deployment-architecture.md))。RDSを使わず、EC2 1台(t3.micro)上でdocker-compose一式を動かす構成に決定 | 工数への影響は軽微(#30の実装対象が明確になっただけ)。予算超過リスク(risk-register R2)への具体的な対応策になる | 承認済み |
| C8 | 環境構築確認(#12)でLaravel/Viteスケルトンを実際にスキャフォールドしCIをグリーンにする過程で、複数の問題を発見・修正: (1)`composer.lock`が編集後の`composer.json`に追従せず`composer install`失敗、(2)ローカルcomposerが素朴にPHPバージョン制約を無視して解決し、PHP8.4必須のsymfonyパッケージがロックされていた(`config.platform.php`を固定して再解決)、(3)最新Laravelがcomposerのセキュリティ勧告ブロックに該当し`--no-blocking`が必要だった、(4)`tests/Unit`・`tests/Feature`が空ディレクトリのままGit追跡されずCIで失敗、(5)ルートの`.gitignore`がLaravel自身の入れ子`.gitignore`と衝突。あわせて、ローカルPHP(8.0.3, XAMPP付属)がLaravel実行に使えなかったため、PHP 8.3.33を別フォルダ(`~/tools/php83`)に展開しユーザーPATHへ優先登録(管理者権限不要) | 実装着手前(#12)の段階で発見・修正できたため後工程への影響はなし。今後のセッションはローカルでも`php`コマンドがPHP8.3系になり、composer/artisanの検証がDocker無しでも(DB接続を除き)可能になる | 承認済み。教訓は[retrospective.md](retrospective.md)フェーズ2に記録 |
| C9 | 「実装(#14)してからテストを書く(#15)」という順序で分割していたIssueが、CLAUDE.md/client-requirements.mdが明記するTDD方針と矛盾していたため統合。#14をエンドポイントごとのRed→Green→Refactorで進める形に再定義し、#15はクローズして#14に統合 | 工数への影響はなし(Issue整理のみ)。実装順序がTDDの精神と一致し、手戻りリスクがむしろ下がる | 承認済み |
| C10 | Task CRUD API実装(#14)完了後に`/code-review`を実施(WBS運用ルール)。6件の指摘のうち3件を独立検証で確認: (1)論理削除済みタスクを親に指定すると孤立タスクが作成できてしまう(`exists`検証がdeleted_atを見ていなかった)、(2)decimal(5,2)上限を超えるestimated_effort/actual_effortが500エラーになる、(3)テストにArrange-Act-Assertコメントが無くcoding-standards.md違反。すべて修正し回帰テストを追加。frontend/package-lock.jsonの無関係な変更混入(1コミット1意図違反)は履歴を書き換えず許容 | 工数への影響は軽微(修正+回帰テスト追加のみ)。実装直後に発見できたため後工程への影響なし | 承認済み |
| C11 | デモ用シーダー作成(#54)の過程で2件発見・修正: (1)`DatabaseSeeder`が削除済みの`App\Models\User`を参照したままで実行するとエラーになる状態だった、(2)開発DB(task_pm_app)とテスト用DB(task_pm_app_testing)が同一だったため、`RefreshDatabase`を使うFeatureテストがシード済みデータで汚染される(件数の決め打ちアサーションが失敗する)ことが判明。`docker/mysql-init/`でテスト用DBを追加作成し、`phpunit.xml`でDB_DATABASEを上書きして分離した | 工数への影響は軽微(修正のみ)。発見が遅れていた場合、シード投入後にテストが原因不明に失敗し続けるという厄介な手戻りになり得た | 承認済み |
| C12 | フェーズ3完了時点の自己適用EVM([schedule.md](schedule.md))でCPI=0.5(見積りの2倍のペース)が判明したことを受け、進捗指標の解釈方針を転換。本プロジェクトの真の制約は「各フェーズを見積り通りに終わらせること」ではなく「時間上限16h」であり、品質(SOLID比較・PMBOK実体験という面接アピールの核)も削れないため、動かせるレバーはスコープのみと判断した。フロントエンド実装(#22)以降をMust(MVP)/Should(余剰時間があれば着手)に仕分け: Must=Task画面(#18, #19)、ESLint/TS strict・最低限のユニットテスト(#20, #21)、主要フローE2E(#25)、リファクタリング・設計比較(#29)、デプロイ・セキュリティレビュー(#34, #53)、PMBOKドキュメント整備・振り返り(#40)。Should=工数入力欄・バグ管理画面・進捗ダッシュボード画面(#47, #48, #49)。Should側3件はバックエンドAPIは実装済みのため、画面が未着手のままクローズしても機能自体は失われない | 工数への影響はなし(優先順位の明確化のみ)。CPI低下を「失敗」ではなく「スコープ判断のタイミング」と再定義したことで、時間逼迫時の判断基準が明確になった | 承認済み(要求元自身の判断によるMVP優先方針) |
| C13 | フロントエンド実装(#18, #19)完了後、`frontend-ci.yml`の型チェックステップ`npx tsc --noEmit`が、ルートの`tsconfig.json`(references専用、`files`が空)に対して実行されると実質チェック対象0件で常に成功してしまい、型エラーがあってもCIが検知できない状態になっていたことが判明。`npx tsc -b`(プロジェクト参照をたどるビルドモード)に修正し、実際に型エラーで失敗することを確認した。あわせて、テストファイル用に新設した`tsconfig.test.json`をtsconfig.app.jsonの`exclude`で除外し、ESLintの`parserOptions.project`にも追加(テスト専用の型定義(jest/jest-domの型)がapp本体のビルドに混入しないようにするため) | 工数への影響は軽微(設定ファイル修正のみ)。C5と同種の「CIのステップ自体が空振りしている」バグであり、発見が遅れていた場合、型エラーを含むコードがCIを通過し続ける状態が続いていた可能性がある | 承認済み |
| C14 | フロントエンド実装フェーズ(#22)完了前の`/code-review`(WBS運用ルール)で、C13の修正自体に不備があることが判明: ルートの`tsconfig.json`の`references`に`tsconfig.test.json`が含まれておらず、`tsc -b`がテストファイルの型エラーを依然検知できていなかった(意図的に型エラーを注入して実証)。`tsconfig.test.json`を`references`へ追加する過程で、module:commonjs(ts-jestのCJS変換向け)とtsc -bの静的型チェック(import.meta構文にはesnextが必要)が同一tsconfig内で両立しないことが判明したため、責務を分離: `tsconfig.test.json`はtsc -b/ESLint用(app設定を継承しjest/jest-dom型のみ追加)、新設の`tsconfig.jest.json`はts-jestの実行時変換専用(module:commonjsへ上書き、`references`対象外)とした | 工数への影響は軽微(設定ファイル修正のみ)。「CIステップの空振り」バグを自ら修正した直後に、その修正自体の検証不足を`/code-review`が捕まえた形であり、レビューの実効性を裏付ける事例になった | 承認済み |
| C15 | 統合・E2Eテスト(#25)着手にあたり、E2Eツールが未決定だったため**Playwright**を採用(理由: TypeScriptとの親和性・CI統合実績いずれも現時点で最も一般的であること。CLAUDE.mdの非機能要件方針と合致)。配置場所はfrontend/配下ではなくリポジトリ直下の`e2e/`(独立したpackage.json)とした。理由はfrontend docker-composeサービスがnode:20-**alpine**(musl libc)でbind mount上に`npm install`するため、同じディレクトリをホスト側(glibc)からもnpm installするとesbuild等のプラットフォーム別バイナリが競合しうるため。実装したE2Eテスト(タスク作成→編集→削除)を実行する過程で、**`php artisan serve`がデフォルトでは1リクエストずつ直列処理する**ため、ブラウザからの複数同時リクエスト(React 19 StrictModeの開発時二重effect等)で最大5秒前後の待ちが発生し、Reactの状態更新が反映される前にE2Eアサーションがタイムアウトする実バグを発見。`docker-compose.yml`のbackendサービスに`PHP_CLI_SERVER_WORKERS=4`(PHP組み込みサーバーの並列ワーカー機能)を設定して緩和した | 工数への影響は軽微(設定ファイル修正+E2Eテスト実装自体)。E2Eテストで初めて顕在化する類のバグ(ユニット/Featureテストでは検知できない、実ブラウザ・実サーバー間の並行性の問題)であり、E2Eテストを追加する目的そのものを裏付ける事例になった | 承認済み |
| C16 | e2e-ci.yml(C15)の初回CI実行で2件発見・修正: (1)ヘルスチェック待ちが最大60秒(2秒×30回)しかなく、キャッシュ無しの`composer install`(126パッケージ)+`migrate`に対して短すぎてタイムアウトした → 最大5分(5秒×60回)に修正。(2)修正後の再実行で、`db`サービスにヘルスチェックが無く`depends_on`がコンテナの起動開始しか待たないため、MySQL自体が接続を受け付ける前に`migrate`が走り`SQLSTATE[HY000] [2002] Connection refused`で失敗する実バグが判明 → `db`に`healthcheck`(`mysqladmin ping`)を追加し、`backend`の`depends_on`を`condition: service_healthy`に変更。ローカルでもボリュームを完全に削除した状態から再現・修正を確認した | 工数への影響は軽微(CI設定の修正のみ)。ローカル開発では`db`が既に起動済みの状態で作業することが多く見過ごされやすい、CI(イメージpull直後の完全新規起動)特有の起動順序レースを実際に踏んで発見できた | 承認済み |
| C17 | 統合・E2Eテストフェーズ(#25)完了前の`/code-review`(WBS運用ルール)で、C16の`healthcheck`修正自体に不備があることが判明: 公式MySQLイメージは初期化中`--skip-networking`(TCP未リッスン)の一時サーバーをUnixソケット経由でのみ起動するが、`mysqladmin ping -h localhost`は`-h localhost`指定時にソケット接続を優先するMySQLクライアントの仕様があり、この一時サーバーに対しても誤って「healthy」と判定しうる状態だった(低確率だが理論上は#25で発生した接続拒否バグが非決定的に再発しうる)。`-h 127.0.0.1 --protocol=tcp`を明示指定してTCP接続を強制するよう修正し、backendが実際に使う経路(DB_HOST=db、TCP)の疎通そのものを確認するようにした。あわせてヘルスチェックの失敗許容時間が短すぎる懸念(5秒×20回=100秒)に対し30回(150秒)へ増やした。ボリュームを完全に削除した状態からの再現テストで修正を確認 | 工数への影響は軽微(設定ファイル修正のみ)。C14と同様、直前に自分で修正したインフラ設定の不備を`/code-review`が捕まえた形。「healthcheck自体を追加する」という一般的な対策も、その実装の正確さまでは自動的に保証されないことを再確認した | 承認済み |
| C18 | フェーズ6完了後、ユーザーが実際にアプリを操作して手動確認したところ、タスク作成フォームに親タスクを指定する項目が存在せず、UIから子タスクを作成する手段が無いことが判明。バックエンドAPI(`POST /api/tasks`の`parent_task_id`)・ER図・API設計はいずれも2階層のタスク階層構造を前提としており、これはスコープ外ではなく#19実装時に見落とした欠落だった(見落とし自体をこの時点までchange-logに記録できていなかった点も課題)。`CreateTaskInput`型に`parent_task_id`を追加し、`TaskForm`にトップレベルタスクを選択できる「親タスク」フィールド(選択肢が無い場合は非表示)を追加してTDDで実装。実際にブラウザで子タスク作成→一覧へのネスト表示までを確認した | 工数への影響は軽微(フロントエンドのみの追加、バックエンド変更なし)。自動テスト(Jest/Pest/Playwright)では「フォームに項目が無いこと」自体は検知できず、ユーザーが実際にアプリを操作して初めて見つかった欠落だった。ユニット/Feature/E2Eいずれのテストも「書かれた仕様通りに動くか」しか検証できず、「必要な機能がそもそも実装されているか」は人間による探索的な動作確認でしか見つからない、という教訓になった | 承認済み |
| C19 | フェーズ6完了後にCPIが0.96まで改善したことを受け、C12でMVP優先方針によりShould(余剰時間があれば着手)として保留していた工数入力欄(#47)・バグ管理画面(#48)・進捗ダッシュボード(#49)に着手。#47は既に#19(タスクフォーム)で対応済みと判明したためクローズのみ。#48・#49をBug/Progress用Vertical Slice(src/features/bug, src/features/progress)として新規実装。App.tsxにタブ切り替えナビゲーションを追加(外部ルーティングライブラリは導入せず) | 工数への影響は当初スコープ外だったが、CPI改善により余裕が生まれた分での着手(C12の方針通り)。実装量としては大きい(2画面分のフルCRUD相当) | 承認済み(要求元自身の判断) |
| C20 | バグ管理画面(#48)実装後の`/code-review`で4件の実バグを発見。`BugPage`は`TaskPage`からコピー適用して作ったため、同じ欠陥が両方に存在していた: (1)削除失敗時、viewモードのJSXブランチが`mutationError`を一切読まずエラー表示規約(coding-standards.md)に違反、(2)作成フォーム表示中に別アイテムを選択しても`isCreating`がクリアされず古い作成フォームが残る、(3)`useTask`/`useBug`がid切替時に`data`をクリアしておらず前のアイテムの詳細が一瞬残る、(4)編集中に「新規作成」→キャンセルすると`detailMode`が残り編集フォームへ意図せず戻る。回帰テストをTDDで先に追加した上でTask/Bug両方を修正。共通の`useFetch`フック抽出によるDRY化(3エージェントが独立に指摘)は工数対効果を踏まえ今回は見送り、検討事項として記録するに留めた | 工数への影響は軽微(修正+回帰テスト追加)。`BugPage`が`TaskPage`のコピーだったため、1つの発見が両方の修正に直結し、レビューの効率と価値を再確認できた | 承認済み |
| C21 | ユーザーがVSCodeのProblemsパネルで報告した2件のIDE診断を調査: (1)`.github/workflows/*.yml`の`on:`キーが未クォートで、YAML 1.1のNorway problem(bare on/off/yes/no等が真偽値と解釈される)によりPyYAMLでパースすると`on`キーがbool trueになることを確認(GitHub Actions自体は特別扱いして動くため実害はなかったが非標準)。全workflowで`"on":`に統一。(2)`CalculateEvmSummary.php`のreadonlyコンストラクタプロパティが構文エラーと報告された件は、実コードの問題ではなく、IDEのPHP構文チェックがまだ古いXAMPP PHP 8.0.3(readonlyはPHP8.1+の構文)を参照していたことが原因と特定(Docker内のPHP8.3では`php -l`で問題無し、PHPStan/Pint/実行テストも継続してパス済み)。`.vscode/settings.json`で`php.validate.executablePath`をプロジェクトが実際に使うPHP8.3へ明示指定し解消 | 工数への影響は軽微(設定ファイルのみ)。IDEの診断は必ずしも実際のコード品質を反映しない(ツール側の設定・スキーマ理解のズレでも発生する)ことを実例で確認できた | 承認済み |
| C22 | デプロイフェーズ([#34](https://github.com/YoshinoriSawaya/task-pm-app/issues/34))着手にあたり、[#53](https://github.com/YoshinoriSawaya/task-pm-app/issues/53)のデプロイ前security-reviewを実施(PR差分が無い運用のため、コードベース全体を対象にsecurity-reviewスキルと同じ方法論(脆弱性洗い出し→独立サブタスクによる誤検知フィルタリング→confidence≥8のみ採用)で実施)。SQLインジェクション・マスアサインメント・CORS誤設定等は確認されなかったが、HIGH severityの実バグを1件発見: ADR-0003(旧版)は「ローカルと同じdocker-compose構成をそのまま起動する」と明記していたが、backendのcommandは`.env`不在時に`backend/.env.example`(`APP_DEBUG=true`・`DB_PASSWORD=secret`固定)へフォールバックする作りだったため、本番で未処理例外が起きるとLaravelのデバッグ画面からDB_PASSWORD・APP_KEY等がそのまま漏えいする経路があった。`docker-compose.prod.yml`を新設し、`APP_ENV=production`・`APP_DEBUG=false`を明示上書き、DB系パスワードはデフォルト値を持たせず未設定なら起動失敗(`${VAR:?message}`)、DBのポート公開も本番のみ無効化する対応をTDDならぬ実機検証(`docker compose config`で上書き結果・必須変数ガードの両方を確認)で修正。ADR-0003・[.env.example](../../.env.example)(新設)を更新 | 工数への影響は軽微(設定ファイル+ADR更新のみ)。デプロイ前に発見できたため実害は無かったが、発見が遅れていれば本番公開直後にDB認証情報が漏えいする重大インシデントになり得た。「ローカルと同じ構成をそのまま使う」という一見合理的なADRの決定こそが、環境差分(dev/prod)を明示的に扱わないリスクを生んでいたことを実例で確認できた | 承認済み |
| C23 | AWSインフラ構築(#30)の実機デプロイ検証で2件のインシデントが発生・解決した。(1) EC2インスタンス(初回はt3.micro)でMySQL+Vite dev server+PHP(4ワーカー)を同時起動した後、SSH自体が「banner exchange中にタイムアウト」する状態に陥り5分以上復旧しなかった。原因の確定はできなかったが、t3.microのRAM(1GB)不足によるスラッシングが濃厚と判断し、インスタンスを削除してt3.small(RAM 2GB)で作り直した(IAMポリシーの`ec2:InstanceType`条件を`t3.small`も許可するよう更新)。(2) t3.small上でも`/api/tasks`が常時500(`SQLSTATE[HY000] [1045] Access denied`)を返す実バグに遭遇。`php artisan migrate`・`tinker`・`db:show`・生PDO等のCLIコマンドはすべて正しいDB_PASSWORDで接続に成功する一方、`php artisan serve`(PHPビルトインサーバー)が処理する実際のHTTPリクエストだけが一貫して失敗することを`/proc/<pid>/environ`の直接比較と一時デバッグルート(`getenv()`/`$_ENV`/`$_SERVER`/`env()`/`config()`を横並びでhash比較)で切り分け、HTTPリクエストのコンテキストではこれら全てがDocker注入値ではなく`.env`ファイルのフォールバック値("secret")を返していることを実証した。ローカル開発ではこのフォールバック値がdocker-compose.ymlの値とたまたま一致していたため、これまで発見されていなかった。対策として、コンテナ起動時にDocker側の環境変数をsedで`.env`ファイルへ明示的に書き込むよう`docker-compose.yml`を修正し、`php artisan serve`の環境変数可視性に依存しない構成にした(修正後、実機で10回連続200を確認) | 工数への影響は中程度(切り分けに時間を要した)。(1)は真因未確定のまま様子見ではなくインスタンスサイズ変更という実利的な判断で進めた。(2)は自動テスト(Pest Feature test、E2E含む)がいずれも`artisan test`または`php artisan serve`を使わないPlaywright実行(webServerなし、既存のdocker-compose起動を前提)のいずれかで、この特定の実行経路(`php artisan serve`が処理する実リクエスト)を通していなかったため、CI・E2Eいずれでも検知できなかった欠陥だった。「本番相当の起動コマンドで実際にHTTPリクエストを送る」検証を経て初めて見つかったバグであり、デプロイ前の実機検証工程そのものの価値を裏付ける事例になった | 承認済み |
| C24 | インフラ層Basic認証([#31](https://github.com/YoshinoriSawaya/task-pm-app/issues/31))を実装。`docker-compose.prod.yml`に`nginx`サービス(`nginx:1.27-alpine`)を追加し、`docker/nginx/default.conf`でBasic認証(`auth_basic`)とリバースプロキシ(`/api/*`→backend:8000、`/up`→backend:8000(認証除外、ヘルスチェック用)、それ以外→frontend:5173)を設定。副次効果として、frontend・backendが同一オリジン(nginxのport 80)で配信されるようになったためCORSが実質不要になった(`frontend`の`VITE_API_BASE_URL`を`docker-compose.prod.yml`で相対パス`/api`に上書き)。backend・frontend単体のポート(8000/5173)は本番ではホストへ公開しない(`ports: !reset []`)。`.htpasswd`はリポジトリにコミットせず、EC2上で`openssl passwd -apr1`により生成し`.gitignore`に追加した | 工数への影響は軽微(設定ファイルのみ、ADR-0003で当初から計画されていた対応)。同一オリジン化によりCORS設定(FRONTEND_URL)がローカル開発専用の構成として整理され、本番の構成がシンプルになった副次的なメリットがあった | 承認済み |
| C25 | 本番動作確認([#32](https://github.com/YoshinoriSawaya/task-pm-app/issues/32))完了後、AWSリソース削除([#33](https://github.com/YoshinoriSawaya/task-pm-app/issues/33))にあたり「削除ではなく停止(stop)で済ませられないか」を検討した。EC2停止はコンピュート課金は止まるが、EBSボリューム(8GB gp3)は停止中も課金が継続すること、パブリックIPがElastic IP未使用のため停止時に解放され次回起動時に変わることを整理して提示した上で、要求元が「削除」を選択(CLAUDE.mdの「使い終わったら速やかに削除し課金を止める」という既定方針を維持。面接等での再利用はローカル起動で足りるとの判断)。EC2インスタンス(t3.small)・EBSボリューム(DeleteOnTermination)・セキュリティグループ・キーペアを削除し、いずれも実際にAWS API(`describe-volumes`/`describe-addresses`/`describe-security-groups`/`describe-key-pairs`)で残存が無いことを確認した。IAMユーザー・ポリシー・AWS Budgetsはリソース課金が発生しないため削除せず維持(次回デプロイ時に再利用可能)。ローカルに保存していたSSH秘密鍵・Basic認証パスワード・.htpasswdも削除した | 工数への影響はごく軽微(削除作業のみ)。「停止」という選択肢を機械的に選ばず、コスト構造(EBSは停止中も課金される)を具体的に提示した上で要求元に判断してもらったことで、CLAUDE.mdの既定方針を維持する意思決定が明確になった | 承認済み(要求元自身の判断) |
| C26 | プロジェクト完了後、SOLID原則の振り返り(retrospective.mdの総括を補足する対話)の中で、`TaskRepositoryInterface`(`backend/app/Features/Task/Application/Ports/TaskRepositoryInterface.php`)がISP(インターフェース分離の原則)に違反していることを確認した。同インターフェースは`save`/`findById`/`allTopLevelWithChildren`/`findByIdWithChildren`/`updatePartial`/`deleteWithChildren`の6メソッドを持つが、各UseCase(`CreateTask`/`ListTasks`/`GetTask`/`UpdateTask`/`DeleteTask`)は実際にはそれぞれ1メソッドしか使用していない(全UseCaseが未使用の5メソッドにも依存させられている状態)。`Bug`スライスの`BugRepositoryInterface`(`save`/`findById`/`all`/`updatePartial`/`delete`の5メソッド)も同型であることを確認した。C10・C14・C17・C20・C22のような「発見して直ちに修正する」対応ではなく、**今回は意図的に修正しない判断とした**: (1)Repositoryパターンでは1エンティティ=1インターフェースに全CRUD操作をまとめる設計が一般的な慣習として広く許容されており、(2)`TaskReader`/`TaskWriter`/`TaskDeleter`等に分割してもこのアプリの規模では恩恵(異なる実装が複数生まれる見込み)が薄く、クラス数増加のコストが上回ると判断した | 工数への影響なし(意図的に未修正)。C20のDRY化見送りと同種の「気づいたが着手しない」判断であり、判断の理由を明記することでSOLID原則の理解が「全部守れているか」ではなく「トレードオフを認識した上で選択できているか」であることを示す事例として記録する | 承認済み(要求元自身の判断) |
